kanban/
├── cmd/        → как запускается приложение
├── internal/   → внутренняя логика (бизнес-код)

всё, что в internal/, нельзя импортировать извне проекта
→ защита архитектуры

cmd/server/main.go - задача, запустить приложение
стартует приложение

читает конфиг

инициализирует БД

падает, если что-то критично

передаёт управление дальше

internal/config/config.go - собрать все env-переменные в одно место
Что там должно быть

структура Config

чтение DATABASE_URL

проверки (env обязателен)

internal/db/postgres.go - инициализировать и вернуть соединение с Postgres
Что там должно быть

функция подключения

Ping

возможный timeout

ответственность ТОЛЬКО за БД