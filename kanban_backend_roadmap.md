# Kanban Backend — Roadmap

Этот документ — **дорожная карта проекта Kanban backend (аналог Jira)**.
Цель: убрать хаос из головы и дать понятный, пошаговый план — *что делать, зачем и в каком порядке*.

---

## 🧠 Большая картина

Проект — **один backend‑сервис**.

Он решает 3 задачи:
1. Хранит данные (PostgreSQL)
2. Отдаёт API (HTTP / JSON)
3. Запускается одинаково везде (Docker)

```
Client (Postman / Frontend)
        ↓ HTTP
Go Backend (handlers → services → db)
        ↓ SQL
PostgreSQL
```

---

## 🧱 Архитектура по слоям

```
┌─────────────────────┐
│ HTTP Handlers       │  ← принимает запросы
├─────────────────────┤
│ Business Logic      │  ← принимает решения
├─────────────────────┤
│ Database Layer      │  ← работает с БД
└─────────────────────┘
```

Каждый слой:
- знает **только про слой ниже**
- не смешивает ответственность

---

## 📦 Сущности (MVP)

### User
- id
- email
- password_hash
- name
- created_at

### Board
- id
- owner_id
- title
- created_at

### Column
- id
- board_id
- title
- position
- created_at

### Card
- id
- column_id
- title
- description
- position
- created_at
- updated_at

Связи:
```
User → Board → Column → Card
```

---

## 🗺️ Roadmap по этапам

### ЭТАП 0 — Подготовка (DONE)
**Цель:** понять, что ты строишь

- [x] выбрать стек (Go, Postgres, Docker)
- [x] определить сущности
- [x] понять общую архитектуру

---

### ЭТАП 1 — Скелет проекта
**Цель:** проект существует и собирается

**Результат:**
- репозиторий создан
- Go‑модуль и структура папок

```
kanban/
├── cmd/server/main.go
├── migrations/
├── go.mod
└── README.md
```

`main.go` пока может быть пустым.

---

### ЭТАП 2 — База данных
**Цель:** данные имеют форму

- [ ] создать `migrations/0001_init.up.sql`
- [ ] создать `migrations/0001_init.down.sql`
- [ ] описать таблицы:
  - users
  - boards
  - columns
  - cards

❌ без тестовых данных
❌ без сложных ALTER

---

### ЭТАП 3 — Подключение БД
**Цель:** Go ↔ Postgres

- [ ] добавить пакет подключения к БД
- [ ] читать `DATABASE_URL`
- [ ] проверить соединение при старте

Результат:
- сервер стартует
- подключение к БД успешно

---

### ЭТАП 4 — HTTP сервер
**Цель:** backend принимает запросы

- [ ] поднять HTTP сервер
- [ ] health endpoint (`GET /health`)

Результат:
- сервер отвечает 200 OK

---

### ЭТАП 5 — Первый API (Boards)
**Цель:** почувствовать прогресс

- [ ] `POST /boards`
- [ ] `GET /boards`

Без:
- auth
- ролей

---

### ЭТАП 6 — Kanban логика (ядро)
**Цель:** Kanban работает

- [ ] создание колонок
- [ ] создание карточек
- [ ] перемещение карточек

Ключевая логика:
- position
- транзакции

---

### ЭТАП 7 — Docker
**Цель:** один запуск — всё работает

- [ ] Dockerfile
- [ ] docker-compose.yml
- [ ] Postgres + backend

Команда:
```bash
docker-compose up
```

---

### ЭТАП 8 — Улучшения (после MVP)

- auth (JWT)
- комментарии
- участники доски
- realtime (WebSocket)
- тесты

---

## 🧭 Правила, чтобы не потеряться

1. **Один этап — одна цель**
2. Не думать о следующем этапе
3. Не сравнивать с Jira
4. Рабочий, но простой код > идеальный

---

## ✅ Текущий фокус

👉 **Всегда работай только над одним пунктом roadmap**

Следующий шаг:
- создать структуру проекта
- или написать первую миграцию

---

> Этот roadmap — твой якорь. Возвращайся к нему, когда появится ощущение хаоса.

